OPENROUTER_API_KEY=
OPENROUTER_MODEL=deepseek/deepseek-v4-flash
GITHUB_TOKEN=
GITHUB_OWNER=rashad-h
GITHUB_REPO=ExpenseTracker

# Optional fallback for direct /api/pr calls without ?pr=<number>.
# The app lists PRs by default and passes the selected PR number.
# GITHUB_PR_NUMBER=1
